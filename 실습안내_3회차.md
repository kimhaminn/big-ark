# 3주차 실습 안내 — 첫 칸을 연다: 객체 저장소

> **빅데이터 엔지니어링 · 3주차 실습 (90분)**
> 투사 화면에는 작업 순서만 나온다. **명령과 확인 방법은 이 문서를 본다.**

> **지난주에 만든 것의 실제 형태에 따라 P0 이 길어질 수 있다.** 그 경우 P4 를 다음 주로 넘긴다.

> ★ **오늘 새로 설치하는 것은 없다.** 객체 저장소 이미지 하나를 받고, **나머지는 전부 그 컨테이너 안에 이미 들어 있는 명령**으로 한다.

---

## 0. 오늘 하는 것

| 과제 | 무엇을 하는가 | 완료 판정 |
|---|---|---|
| **P0** | 지난주 것 확인 | 구성 파일을 열어 놓고 P1 을 시작할 수 있다 |
| **P1** | 구성 파일에 객체 저장소를 더한다 | **컨테이너를 지웠다 다시 올려도 버킷이 남는다** |
| **P2** | 안 되는 것을 해 본다 | "왜 이름 바꾸기가 비싼가"에 답할 수 있다 |
| **P3** | **키를 설계한다** | 결정 기록에 **「무엇을 포기했는가」**가 적혀 있다 |
| **P4** | 수명을 정한다 — 또는 정하지 않는다 | 답에 규모 수치가 한 줄이라도 들어 있다 |

**오늘 남기는 것 넷** — 객체 저장소가 들어간 구성 파일 · 버킷과 **키 규약** · 결정 기록 1건 · 봉인 답안 파일 1개

★ **오늘 처음으로 시스템의 칸이 하나 켜진다.** 지난 두 주는 담을 그릇을 만들었다.

> **오늘 넣는 내용은 버려도 된다. 넣는 방식은 버리지 않는다.**
> 5주차부터 센서 데이터가 **오늘 정한 키 구조 그대로** 들어온다.

---

## 터미널과 경로

**아래 명령은 PowerShell 에서도 WSL 터미널에서도 같다.** 지난주와 같이 **작업 폴더 경로 표기**만 다르다.

| 터미널 | 지난주 폴더로 가는 법 |
|---|---|
| PowerShell | `cd d:\dev\big-ark\week02` |
| WSL | `cd /mnt/d/dev/big-ark/week02` |

`D:` 가 아닌 곳에 만들었으면 자기 경로로 바꾼다.

---

## 객체 저장소 이미지 — 각자 받는다

```bash
docker pull quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z
```

받는 동안 P0 을 진행한다. **먼저 이 명령을 걸어 두고 아래로 내려간다.**

> ★ **주소 앞의 `quay.io/` 를 빼지 않는다.** 검색하면 나오는 `minio/minio` 는 **지금 받아지지 않는다**(2026-09-14 확인). MinIO 는 무료판 이미지 배포를 멈췄고, 위 주소에 **마지막으로 공개된 판**이 남아 있다.
> **태그를 날짜까지 고정했다.** 지난주 결정 기록에서 다룬 그 판단이다 — 더 이상 새 판이 나오지 않으므로 이번에는 고정하는 쪽에 잃을 것이 없다.

---

## P0. 지난주 것이 살아 있는지 확인 — 10분

### ① 컨테이너가 도는가

```bash
docker run --rm hello-world
```

### ② 지난주 폴더를 연다

```bash
cd d:\dev\big-ark\week02
ls
```

`compose.yml` · `.env` · `.gitignore` 가 보이면 된다. **오늘은 이 `compose.yml` 을 고친다. 새 파일을 만들지 않는다.**

### ③ 문서 3층과 인덱스가 있는지

`d:\dev\big-ark\docs` 아래 `결정` · `구조` · `절차` 와 인덱스를 확인한다. **P3 의 결정 기록이 `결정` 에 붙는다.**

### ④ 지난주 연습용 컨테이너가 내려갔는지

```bash
docker compose down          # 지난주 것이 떠 있으면 내린다
docker ps -a                 # lab-db 가 남아 있지 않아야 한다
```

> **남아 있으면 `docker rm -f lab-db`.** 지난주 연습용 DB 는 오늘 쓰지 않는다.

**지난주에 못 한 것이 있으면 지금 말한다.** 여기서 닫고 간다.

---

## P1. 칸을 연다 — 구성 파일을 고친다 — 20분

### ① 연습용 DB 를 지우고 객체 저장소를 쓴다

`compose.yml` 을 편집기로 연다. **지난주의 `db:` 블록을 통째로 지우고** 그 자리에 아래를 쓴다.

> **왜 지우는가.** 지난주 DB 는 선언을 연습하려고 띄운 것이고 **TFDS-Edu 의 칸이 아니다.** 오늘부터 이 파일은 시스템의 칸만 담는다. **지운 블록은 지난주 커밋에 그대로 남아 있다** — 파일에서 지우는 것이 기록을 지우는 것은 아니다.

```yaml
services:
  storage:                                  # 서비스 이름 — 명령에서 이 이름으로 부른다
    image: quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"                         # API — 프로그램이 쓴다
      - "9001:9001"                         # 콘솔 — 사람이 본다
    environment:
      MINIO_ROOT_USER: ${STORAGE_USER}      # 왼쪽은 MinIO 가 정한 이름, 오른쪽은 내가 정한 이름
      MINIO_ROOT_PASSWORD: ${STORAGE_PASSWORD}
    volumes:
      - storage_data:/data                  # ★ 이 줄이 오늘의 통과선이다

volumes:
  storage_data:                             # 이름 있는 볼륨을 선언한다 — 맨 왼쪽에서 시작한다
```

★ **`environment:` 의 왼쪽 이름은 고칠 수 없다.** 지난주 `POSTGRES_PASSWORD: ${DB_PASSWORD}` 와 같은 모양이다 — 왼쪽은 **이미지가 읽기로 정해 둔 이름**이고, 오른쪽 `${ }` 안만 내가 정한다. **왼쪽을 틀리면 컨테이너는 멀쩡히 뜨는데 로그인만 안 된다.**

### ② 자격증명은 파일 밖에 둔다

`.env` 를 **편집기로 연다.** 지난주 `DB_PASSWORD` 줄이 있다. 그 줄을 지우고 두 줄을 쓴다.

```
STORAGE_USER=admin
STORAGE_PASSWORD=각자정한다8자이상
```

> ★ **비밀번호는 8자 이상.** 짧으면 컨테이너가 뜨자마자 꺼진다.
> ★ **값 뒤에 공백을 넣지 않는다.** 지난주와 같다.

`.gitignore` 에 `.env` 가 **이미 있는지만 확인한다.** 지난주에 넣었다.

> **지난주 판단의 연장이다.** 무엇을 형상관리 대상으로 둘 것인가 — 비밀번호는 아니다.

### ③ 올린다

```bash
docker compose up -d
docker compose ps            # STATUS 가 Up(running) 인지
```

브라우저에서 콘솔에 붙는다 — `http://localhost:9001`
**`.env` 에 적은 값으로 로그인한다.**

### ④ 명령줄 도구를 준비한다

**객체 저장소를 다루는 명령 `mc` 는 컨테이너 안에 이미 있다.** 저장소 주소와 계정을 한 번 알려 준다.

```bash
docker compose exec storage sh -c 'mc alias set local http://localhost:9000 $MINIO_ROOT_USER $MINIO_ROOT_PASSWORD'
```

`Added 'local' successfully.` 가 나오면 된다.

> ★ **비밀번호를 치지 않았다.** 작은따옴표 안의 `$MINIO_ROOT_USER` 는 **컨테이너 안에서** 채워진다 — `.env` 로 뺀 값을 명령줄에 다시 적지 않는 방법이다. **따옴표 모양을 바꾸지 않는다.**

### ⑤ 버킷을 만든다

```bash
docker compose exec storage mc mb local/tfds-raw
docker compose exec storage mc ls local/
```

콘솔을 새로고침해 `tfds-raw` 가 보이는지도 본다.

### ⑥ ★ 지웠다 다시 올린다

```bash
docker compose down          # 컨테이너를 지운다 (볼륨은 남는다)
docker compose up -d
```

**컨테이너가 새로 만들어졌으므로 ④ 의 준비도 사라졌다.** ④ 를 한 번 더 친 뒤 확인한다.

```bash
docker compose exec storage sh -c 'mc alias set local http://localhost:9000 $MINIO_ROOT_USER $MINIO_ROOT_PASSWORD'
docker compose exec storage mc ls local/
```

**`tfds-raw` 가 그대로 있으면 통과다.**

> **완료 판정은 「지금 떴다」가 아니라 「다시 올려도 있다」이다.**
> 오늘부터 산출물이 다음 주의 입력이 되므로, 다음 주에 없으면 안 만든 것과 같다.
> ★ **남은 것과 사라진 것을 구분한다.** 볼륨에 둔 버킷은 남았고, 컨테이너 안에 둔 ④ 의 준비는 사라졌다. **지난주 P4 끝에서 물은 질문의 답이 이것이다.**

### 판단 — 근거를 말할 수 있어야 한다

**볼륨을 어느 쪽으로 둘 것인가.**

| | 이름 있는 볼륨 | 호스트 경로 바인드 |
|---|---|---|
| 예시 | `storage_data:/data` | `./data:/data` |
| 관리 | 도커가 알아서 둔다 | 내가 정한 자리에 그대로 |
| 눈으로 보기 | 안 보인다 | 탐색기에서 그대로 보인다 |
| 옮기기 | 명령이 필요하다 | 폴더째 복사 |

**정답은 없다.** 다만 고른 이유를 말할 수 있어야 한다.

### ⑦ 커밋한다

```bash
git status                   # .env 가 목록에 없어야 한다
git add .
git commit -m "week03: 객체 저장소 칸을 연다"
```

### 막혔을 때

1. **이미지를 못 받는다 · `denied` · `not found`** → 주소 앞 **`quay.io/`** 와 태그 전체를 확인한다
2. **포트가 이미 쓰이고 있다** → 지난주 컨테이너가 떠 있다. `docker ps -a` 로 보고 `docker rm -f lab-db`
3. **`orphan containers` 경고가 나온다** → 파일에서 지운 `db` 의 컨테이너가 남아 있다는 뜻이다. `docker rm -f lab-db`
4. **컨테이너가 뜨자마자 꺼진다** → **비밀번호가 8자보다 짧다.** `docker compose logs storage` 에 이유가 나온다
5. **콘솔 로그인이 안 된다** → `environment:` **왼쪽 이름**이 `MINIO_ROOT_USER` · `MINIO_ROOT_PASSWORD` 인지 본다. 틀리면 컨테이너는 뜨지만 내 값이 안 들어간다. 그다음 `docker compose config` 로 오른쪽 값이 채워졌는지 본다
6. ★ **다시 올렸더니 버킷이 사라졌다** → **볼륨을 안 붙였다.** `volumes:` 두 곳(서비스 안·파일 맨 아래)을 확인한다
7. **`mc` 가 `Access Denied` · `Unable to list` 를 낸다** → **④ 를 다시 친다.** 컨테이너를 새로 만들 때마다 필요하다

---

## P2. 안 되는 것을 해 본다 — 20분

> ★ **이 과제는 기대와 다르게 되는 것이 정답이다.** 아래 ②③④ 는 **파일시스템이라면 됐을 일이 여기서는 다르게 되는 것을 확인하는 작업**이다.
> "이상한데요"라고 말하는 순간이 통과 지점이다. 손 들지 않아도 된다.

**명령은 전부 이 모양이다** — `docker compose exec storage mc <명령> local/<버킷>/<키>`

### ① 파일을 `a/b/c.txt` 라는 이름으로 넣는다

```bash
docker compose exec storage sh -c 'echo hello | mc pipe local/tfds-raw/a/b/c.txt'
```

콘솔에서 `tfds-raw` 를 연다. **`a` 폴더 안에 `b` 폴더가 있는 것처럼 보인다.**

### ② 같은 이름에 다시 넣는다

```bash
docker compose exec storage sh -c 'echo world | mc pipe local/tfds-raw/a/b/c.txt'
docker compose exec storage mc cat local/tfds-raw/a/b/c.txt
```

> **`hello world` 가 아니라 `world` 다.** 이어 붙는 것이 없고 통째로 바뀐다.

### ③ 이름을 바꿔 본다

```bash
docker compose exec storage mc mv local/tfds-raw/a/b/c.txt local/tfds-raw/a/b/d.txt
```

**옮긴 바이트 수가 찍힌다** — `6 B / 6 B` 나 `Transferred` 표처럼. 이름만 바꿨는데 전송이 일어났다.

> **그것이 바로 이름 바꾸기의 실제 동작이다.**
> 파일시스템은 이름표만 바꾸지만 여기서는 **내용을 통째로 복사한 뒤 원본을 지운다.** 지금은 6바이트라 순식간이지만, 파일이 클수록 개수가 많을수록 비싸진다.

### ④ 폴더만 남겨 본다

`a/b/` 안의 마지막 파일을 지운다. **폴더는 남을까?**

```bash
docker compose exec storage mc rm local/tfds-raw/a/b/d.txt
docker compose exec storage mc ls local/tfds-raw/
```

> **`a/` 가 같이 사라졌다. 정상이다.**
> 폴더는 처음부터 없었다. 있던 것은 **`a/b/d.txt` 라는 이름의 객체 하나**뿐이었고, 화면과 `mc` 가 이름 속 `/` 를 보고 폴더처럼 그려 준 것이다. **객체가 없어지자 그릴 것도 없어졌다.**

### ⑤ 인증 없이 주소로 요청해 본다

브라우저 새 탭에 이 주소를 넣는다 — `http://localhost:9000/tfds-raw/`

> **`AccessDenied` 가 적힌 XML 이 나온다. 정상이다.**
> 핵심은 거절당했다는 것이 아니라 **거절한 쪽이 서버라는 것**이다. 이것은 디스크를 붙인 것이 아니라 **주소로 보내는 요청**이고, 콘솔과 `mc` 는 그 요청에 계정을 실어 보내 줄 뿐이다.

### 완료

**"왜 이름 바꾸기가 비싼가"에 한 문장으로 답할 수 있으면 통과다.** 세 성질(폴더가 없다 / 요청이다 / 통째로 바뀐다) 중 하나만 말해도 된다.

### 막혔을 때

1. ★ **폴더가 안 사라진다** → 콘솔의 폴더 만들기나 `mc mb` 로 폴더를 따로 만들었다면, 도구가 **이름 끝이 `/` 인 0바이트 객체**를 하나 만들어 둔 것이다. **그것도 폴더가 아니라 객체다** — `mc ls` 에 `0B` 로 보인다
2. **`mc` 가 `Access Denied` 를 낸다** → P1 ④ 를 다시 친다

---

## P3. 키를 설계한다 — 25분 ★오늘의 중심

> **되돌릴 수 없는 결정이다.** P2 에서 봤듯 이름은 나중에 못 바꾼다.
> 양식은 `키설계_양식.md` 를 **`docs\결정\` 에 복사해** 쓴다.

### ① 넣을 것의 형태를 정한다

TFDS-Edu 는 터널 센서 데이터를 받는다. **한 덩어리를 무엇으로 할 것인가.**

- 센서 하나의 **한 시간치**
- 센서 하나의 **하루치**
- **이벤트 한 건**

정답은 없다. 다만 **한 덩어리가 너무 작으면 개수가 폭발하고, 너무 크면 일부만 읽을 수 없다.**

### ② 후보 셋을 놓고 고른다

| 후보 | 무엇이 쉬워지는가 | 무엇이 어려워지는가 |
|---|---|---|
| `센서/날짜/시각` | 센서 하나의 이력을 통째로 훑기 | **하루치 전체**를 보려면 센서 수만큼 돌아야 한다 |
| `날짜/센서/시각` | 특정 날짜 전체 훑기 · **날짜 단위 삭제** | 센서 하나의 장기 이력이 흩어진다 |
| `날짜/시각/센서` | 시간 구간 훑기 | 센서 축 조회가 가장 비싸다 |

**기준은 「지금 넣기 편한 순서」가 아니라 「나중에 무엇으로 찾을 것인가」다.**

### ③ 고른 구조로 파일을 몇 개 넣는다

예시 — `날짜/센서/시각` 을 골랐다면. **자기가 고른 구조면 경로 조각의 순서를 바꿔 친다.**

```bash
docker compose exec storage sh -c 'echo 24.1,0.02 | mc pipe local/tfds-raw/2026-09-14/sensor-01/09.txt'
docker compose exec storage sh -c 'echo 24.3,0.02 | mc pipe local/tfds-raw/2026-09-14/sensor-01/10.txt'
docker compose exec storage sh -c 'echo 23.9,0.01 | mc pipe local/tfds-raw/2026-09-14/sensor-02/09.txt'
docker compose exec storage sh -c 'echo 24.0,0.02 | mc pipe local/tfds-raw/2026-09-15/sensor-01/09.txt'
```

### ④ 접두어로 목록을 뽑는다

**날짜로 뽑는다.**

```bash
docker compose exec storage mc ls --recursive local/tfds-raw/2026-09-14/
```

**센서로 뽑는다.**

```bash
docker compose exec storage mc ls --recursive local/tfds-raw/sensor-01/
```

> **하나는 나오고 하나는 아무것도 안 나온다. 오류도 없다.** 그것이 ②에서 고른 결과다.
> **객체 저장소에는 검색이 없다.** 접두어로 목록을 뽑는 것이 전부이고, 그래서 **접두어 순서가 곧 조회 능력**이 된다.

**그래도 센서로 찾고 싶다면** —

```bash
docker compose exec storage mc find local/tfds-raw --path "*sensor-01*"
```

> **나온다. 다만 버킷 전체를 한 건씩 훑어서 나온 것이다.** 지금은 네 건이라 차이가 안 보이지만, 센서 수 × 날짜 수만큼 늘어나면 이 명령이 비용이 된다. **키 구조가 안 맞는 질의는 「안 된다」가 아니라 「비싸다」로 나타난다.**

### ⑤ 결정 기록을 쓴다

`키설계_양식.md` 를 채워 `docs\결정\` 에 넣고 **인덱스에 한 줄 더한다.**

```bash
git add .
git commit -m "week03: 키 설계 결정 기록"
```

### 완료

★ **결정 기록에 「무엇을 포기했는가」가 적혀 있으면 통과다.**
**어느 키를 골랐는지는 채점하지 않는다.** 고른 것만 적고 버린 둘을 안 적으면 미통과다.

> 강의에서 본 네 시스템의 공통점이 하나 있었다 — **검토했다가 버린 대안이 어디에도 안 남아 있다.**
> 오늘 여러분이 버린 둘을 적으면, 그 빈자리를 메우는 일을 직접 해 보는 것이다.

### 막혔을 때

1. ★ **접두어로 뽑았는데 아무것도 안 나오거나 엉뚱한 게 딸려 온다** → **도구 문제가 아니라 설계 결과다.** 그것을 확인하는 것이 이 과제다
2. **넣었는데 목록에 없다** → 버킷 이름과 **경로 조각의 순서**를 확인한다 — `mc ls --recursive local/tfds-raw/` 로 전체를 본다

---

## P4. 수명을 정한다 — 또는 정하지 않는다 — 15분

> 양식은 `봉인답안_양식.md` 를 쓴다. ★ **리포지토리에 넣지 않는다** — 넣으면 누구 것인지 드러난다. **파일 이름에도 이름을 쓰지 않는다.**
> 작업 폴더 밖(바탕화면 등)에 복사해 채우고, **강사가 알려주는 곳에 파일로 제출한다.**

### ① 규모를 센다

지금 설계한 대로 데이터가 들어온다면 얼마나 쌓이는가.

```
센서 ____개 × 하루 ____건 × ____일 = ____건
```

한 건의 크기를 어림하면 용량도 나온다.

### ② 얻는 것과 드는 것을 적는다

계층을 나눈다면(최신 / 최근 / 영구처럼) —

- **얻는 것** — 한 줄
- **드는 것** — 한 줄

### ③ 답한다

**"지금 규모에서 계층을 나눌 필요가 있는가."**

★ **「아니오」가 정답일 수 있다.**
강의에서 본 네 시스템 중 **객체 저장소를 둔 곳이 하나도 없었고, 그 판단이 규모에 맞았다.**

★ 다만 **근거 없는 「아니오」는 근거 없는 「예」와 같다.** 둘 다 미통과다.

### 완료

**답에 규모 수치가 한 줄이라도 들어 있으면 통과다.** 판단의 방향은 채점하지 않는다.

---

## 오늘 남긴 것 확인

- [ ] 구성 파일에서 `db` 를 지우고 `storage` 를 넣었다 (새 파일을 만들지 않았다)
- [ ] `.env` 에 두 줄을 넣었고, `.gitignore` 에 `.env` 가 있다
- [ ] **다시 올려도 버킷이 남는다**
- [ ] 버킷 `tfds-raw` 와 **키 규약**이 정해졌다
- [ ] 결정 기록 1건 — **버린 대안 둘이 적혀 있다**
- [ ] 봉인 답안 파일 1개 — **리포지토리 밖에서 제출했다**
- [ ] 커밋했다

> **컨테이너를 내리지 않는다.** 다음 주에 이 위에 브로커를 더한다.

---

## 다음 주에 이어지는 것

| 오늘 만든 것 | 다음 주 |
|---|---|
| 구성 파일 (`storage` 서비스) | **여기에 브로커를 더한다.** 브로커는 `9092` 를 쓰므로 오늘 포트와 겹치지 않는다 |
| 버킷과 키 규약 | 5주차부터 **실제 센서 데이터**가 이 규약으로 들어온다 |
| 결정 기록 | 회차마다 한 건씩 쌓인다 |
| `mc` 준비 (P1 ④) | **컨테이너를 새로 만들면 사라진다.** 다음 주에 `mc` 를 쓸 일이 있으면 한 번 다시 친다 |
