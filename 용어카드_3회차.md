# 3주차 기억할거리 — 용어 카드

> **빅데이터 엔지니어링 · 3주차** — 거인들의 발자국: 계보와 저장 패러다임 전환
> ★ **논문 이름과 구현 이름이 다르다.** 이 카드에서 가장 자주 섞이는 것이 그 둘이므로 맨 위 두 줄에 갈라 두었다.

---

## 논문과 구현

| 용어 | 한 줄 정의 | 왜 중요한가 |
|---|---|---|
| **GFS · MapReduce · BigTable** | 구글이 공개한 **세 편의 논문** | 설계이지 물건이 아니다. **구현이 아니라 방법을 공개했다** |
| **HDFS · 하둡 MapReduce · HBase** | 그 논문을 따라 만든 **오픈소스 구현** | **이름이 다르다.** 섞어 부르면 계보가 안 보인다 |

---

## 분산 저장

| 용어 | 한 줄 정의 | 왜 중요한가 |
|---|---|---|
| **블록** | 파일을 자른 단위. 복제의 단위이기도 하다 | 조각이 크면 관리가 싸고, 작으면 병렬이 는다 |
| **복제 계수** | 같은 블록을 몇 벌 둘 것인가 | 기본 3벌 — **동시에 두 대가 죽어도 남는다** |
| **단일 지점** | 그곳이 멈추면 전체가 멈추는 자리 | 데이터는 여러 벌인데 **이름은 한 벌**이었다 |
| **데이터 지역성** | 데이터가 있는 곳에서 계산하는 배치 | 전제는 **「네트워크가 디스크보다 느리다」**였다 |
| **합의 계층** | 여럿이 같은 값을 알게 하는 전담 계층 | **데이터가 아니라 합의를 담는다.** 크게 담으면 병목이 된다 |

---

## 객체 저장소

| 용어 | 한 줄 정의 | 왜 중요한가 |
|---|---|---|
| **객체 저장소** | 평면 키 공간에 **통째로 넣고 통째로 꺼내는** 저장소 | 디렉터리가 없다. 부분 수정이 없다 |
| **평면 키 공간** | 경로처럼 보이지만 실제로는 **이름 하나** | 폴더는 **화면이 만드는 착시**다. 그래서 이름 바꾸기가 비싸다 |
| **접두어 조회** | 이름 앞부분이 같은 것만 걸러 뽑는 것 | **검색이 없다.** 그래서 **접두어 순서가 곧 조회 능력**이다 |
| **테이블 계층** | 파일 더미가 하나의 표라는 약속을 **파일로 적는 층** | 저장소도 엔진도 갈아 끼울 수 있게 한다 |
| **계산·저장 분리** | 계산 자원과 저장 자원을 **따로 늘리는** 구조 | **오늘의 결론.** 1주차에 이미 본 그림이다 |

---

## 오늘의 판단 기준

**분산 저장을 검토할 조건 셋** — 둘 이상 걸리면 본다.

1. 원본을 **버릴 수 없는가** (규정·재분석 요구)
2. 읽는 쪽이 **둘 이상인가**
3. 용량 증가가 **계단형인가**

**객체 저장소가 대신해 주지 않는 것** — 부분 수정 · 값싼 이름 바꾸기 · 파일시스템 의미론

---

**한 줄로 남길 것**

> 오늘 배운 구조는 지금 새로 만들지 않는다.
> **그런데 그것이 무엇이 부족해서 나왔는지를 모르면 다음 세대도 고를 수 없다.**
